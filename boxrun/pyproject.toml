[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "boxrun"
version = "1.1.0"
description = "A boxedLANG interpreter"

dependencies = [
    "colorama"
]
requires-python = ">=3.10"

[project.scripts]
boxrun = "main:main"
